---
title: "第04章：处理用户输入"
description: "用 Zod schema 为工具设计健壮的输入验证——让你的工具既对 AI 友好，又对错误免疫。从 z.string 到 z.enum，从 optional 到 strictObject，通过 Claude Code 源码中的真实例子掌握 Zod schema 设计。"
tags: [zod, schema, validation, input, tool-design, strictObject, enum, optional]
date: 2026-05-10
---

# 第04章：处理用户输入

上一章，你创建了自己的第一个工具。它接收输入、做事情、返回结果。但你有没有想过一个问题：如果 AI 传过来的参数不对呢？

比如你的工具期望一个文件路径，AI 传了一个空字符串。或者你期望一个枚举值 `"replace"`，AI 传了一个 `"overwrite"`。再或者你定义了一个可选参数，AI 反而把它填成了 `"undefined"`——注意，是字符串 `"undefined"`，不是真正的 undefined。

这些不是假设。Claude Code 的开发者每天都在处理这些问题。AI 是一个概率模型，它生成的 JSON 参数有时候就是会出格。你的工具需要一道防线，把不合法的输入挡在门外。

这道防线就是 **Zod schema**。

这一章，我们从 Claude Code 源码中的真实工具出发，学习如何用 Zod 设计健壮的输入验证。

---

## 目标

完成这一章后，你将能够：

1. 理解 Claude Code 中 Zod schema 的常见模式：`z.string`、`z.enum`、`z.optional`、`z.array`
2. 知道什么时候用 `z.strictObject` 而不是 `z.object`
3. 学会用 `.describe()` 为 AI 写参数说明
4. 理解 `lazySchema` 的作用
5. 升级上一章的工具，加入完整的输入验证

---

## 动手

### 第一步：最简单的 schema——两个必填字符串

让我们从源码中最简洁的 schema 开始。`FileWriteTool` 是写文件的工具，它的输入只有两个字段：

```typescript
// 文件：src/tools/FileWriteTool/FileWriteTool.ts，第56-65行
const inputSchema = lazySchema(() =>
  z.strictObject({
    file_path: z
      .string()
      .describe(
        'The absolute path to the file to write (must be absolute, not relative)',
      ),
    content: z.string().describe('The content to write to the file'),
  }),
)
```

逐行拆解：

**`z.strictObject`**——定义一个对象，并且**不允许额外字段**。如果 AI 传了 `{ file_path: "...", content: "...", encoding: "utf8" }`，那个多出来的 `encoding` 会被拒绝。为什么？因为工具应该只接收自己能理解的参数。多余字段可能是 AI 的"幻觉"——它以为这个工具支持编码选项，其实不支持。`strictObject` 把这些幻觉挡在门外。

**`z.string()`**——声明这个字段的值必须是字符串。如果 AI 传了 `{ file_path: 123 }`（一个数字），Zod 会报错。

**`.describe('...')`**——这是 Zod 的元数据功能，给字段加一段人类可读的描述。但它的作用不止于此。Claude Code 会把 schema 转换成 JSON Schema 发送给 AI。这段 `.describe()` 里的文字就是 AI 看到的参数说明。AI 读到 "must be absolute, not relative"，就知道该传绝对路径而不是相对路径。**好的 `.describe()` 就是好的 AI 提示词。**

注意看 `file_path` 的描述。它不仅说了"是什么"（文件路径），还特别强调了"不是什么"（不能是相对路径）。这是写工具描述的一个好习惯：**告诉 AI 边界在哪里，比告诉它规则是什么更有效。**

### 第二步：可选字段和默认行为

`FileWriteTool` 太简单了——两个必填字段，没有可选余地。让我们看一个有可选字段的例子。`GlobTool` 用来按模式搜索文件：

```typescript
// 文件：src/tools/GlobTool/GlobTool.ts，第26-36行
const inputSchema = lazySchema(() =>
  z.strictObject({
    pattern: z.string().describe('The glob pattern to match files against'),
    path: z
      .string()
      .optional()
      .describe(
        'The directory to search in. If not specified, the current working directory will be used. IMPORTANT: Omit this field to use the default directory. DO NOT enter "undefined" or "null" - simply omit it for the default behavior. Must be a valid directory path if provided.',
      ),
  }),
)
```

`pattern` 是必填的，`path` 是可选的。注意 `.optional()` 的位置——它链在 `z.string()` 后面，意思是"这个字段可以不存在"。

但重点在 `path` 的 `.describe()` 上。这段描述很长，而且包含了一个 `IMPORTANT` 警告。为什么？因为 AI 有一个常见错误：当它不想传某个可选参数时，它不会直接省略这个字段，而是把它设为字符串 `"undefined"` 或 `"null"`。这段描述就是专门防止这个问题的——它明确告诉 AI "DO NOT enter undefined or null, simply omit it"。

这是设计工具 schema 的一条重要经验：**你以为的"可选"对 AI 来说不是理所当然的。你需要用明确的文字告诉它怎么处理可选字段。**

### 第三步：枚举——限制可选值

有时候，一个参数只能取几个固定值。比如搜索工具的输出模式只能是"内容"、"文件列表"或"计数"。`GrepTool` 用 `z.enum` 来表达这个约束：

```typescript
// 文件：src/tools/GrepTool/GrepTool.ts，第53-57行
output_mode: z
  .enum(['content', 'files_with_matches', 'count'])
  .optional()
  .describe(
    'Output mode: "content" shows matching lines, "files_with_matches" shows file paths, "count" shows match counts. Defaults to "files_with_matches".',
  ),
```

`z.enum` 接收一个字符串数组，表示这个字段只能取这几个值。如果 AI 传了 `output_mode: "summary"`，Zod 会直接拒绝，并返回一条错误信息告诉 AI 可选值是什么。

再看 `NotebookEditTool` 的例子，它有两个 enum 字段：

```typescript
// 文件：src/tools/NotebookEditTool/NotebookEditTool.ts，第43-56行
cell_type: z
  .enum(['code', 'markdown'])
  .optional()
  .describe(
    'The type of the cell. If not specified, it defaults to the current cell type. If using edit_mode=insert, this is required.',
  ),
edit_mode: z
  .enum(['replace', 'insert', 'delete'])
  .optional()
  .describe(
    'The type of edit to make. Defaults to replace.',
  ),
```

注意 `cell_type` 的描述里有一条额外的规则："如果 edit_mode 是 insert，那 cell_type 是必填的。" 这种跨字段的条件约束 Zod 本身表达不了（它只能验证单个字段的类型），但你可以写在 `.describe()` 里让 AI 知道，然后在 `validateInput()` 里做真正的运行时校验。

### 第四步：数组——重复元素

有些工具需要接收一个列表。`TodoWriteTool` 是管理任务清单的工具，它的输入是一个 todo 数组：

```typescript
// 文件：src/tools/TodoWriteTool/TodoWriteTool.ts，第13-17行
const inputSchema = lazySchema(() =>
  z.strictObject({
    todos: TodoListSchema().describe('The updated todo list'),
  }),
)
```

`TodoListSchema` 是一个更复杂的数组 schema（定义在 `src/utils/todo/types.ts`），但从概念上讲就是 `z.array(z.object({...}))`——一个对象数组。

在 `FileWriteTool` 的输出 schema 里，我们也能看到数组的用法：

```typescript
// 文件：src/tools/FileWriteTool/FileWriteTool.ts，第78-79行
structuredPatch: z
  .array(hunkSchema())
  .describe('Diff patch showing the changes'),
```

`z.array(某schema)` 表示"一个由符合某 schema 的元素组成的数组"。嵌套的 `hunkSchema()` 定义了每个 diff 块的结构。

### 第五步：lazySchema——为什么要包一层

你可能注意到，所有这些 schema 都被包在一个 `lazySchema()` 里。这个包装器的实现极其简单：

```typescript
// 文件：src/utils/lazySchema.ts
export function lazySchema<T>(factory: () => T): () => T {
  let cached: T | undefined
  return () => (cached ??= factory())
}
```

它做的事情是：把 schema 的创建推迟到第一次被访问的时候，并且缓存结果。为什么要这样做？

因为工具在模块加载时就被定义了。如果 schema 引用了其他模块的类型（比如 `hunkSchema` 引用了 `gitDiffSchema`，`TodoListSchema` 引用了 todo 类型），而这些模块还没加载完，就会出现循环依赖问题。`lazySchema` 把 schema 的创建延迟到运行时，等所有模块都加载好了再执行。

这不是 Zod 的功能，而是 Claude Code 的工程实践。在你的工具里也建议使用它——养成习惯就好。

### 第六步：semanticBoolean 和 semanticNumber——AI 的坏习惯

AI 生成 JSON 时有一些让人头疼的坏习惯。它偶尔会把布尔值写成字符串——`"replace_all": "false"` 而不是 `"replace_all": false`。或者把数字写成字符串——`"head_limit": "30"` 而不是 `"head_limit": 30`。

`z.boolean()` 会拒绝字符串 `"false"`，`z.number()` 会拒绝字符串 `"30"`。用 `z.coerce` 可以强制转换，但它太宽松了——`z.coerce.boolean()` 把任何非空字符串都转成 `true`（包括 `"false"`！），`z.coerce.number()` 把空字符串转成 `0`。

Claude Code 的解决方案是两个专门的包装器：`semanticBoolean` 和 `semanticNumber`。

```typescript
// 文件：src/utils/semanticBoolean.ts
export function semanticBoolean<T extends z.ZodType>(
  inner: T = z.boolean() as unknown as T,
) {
  return z.preprocess(
    (v: unknown) => (v === 'true' ? true : v === 'false' ? false : v),
    inner,
  )
}
```

它做的事很简单：在 Zod 验证之前，先把字符串 `"true"` 转成 `true`，把 `"false"` 转成 `false`，其他值原样传给内层的 schema。这样 AI 传 `"false"` 也能通过验证，而不会出现 `z.coerce.boolean()` 那种把 `"false"` 当成 `true` 的荒谬错误。

`semanticNumber` 的思路类似，只接受合法的数字字符串（如 `"30"`、`"-5"`），拒绝非数字字符串。

GrepTool 大量使用了这两个包装器：

```typescript
// 文件：src/tools/GrepTool/GrepTool.ts
'-B': semanticNumber(z.number().optional()).describe(
  'Number of lines to show before each match',
),
'-i': semanticBoolean(z.boolean().optional()).describe(
  'Case insensitive search',
),
```

这是另一个值得学习的工程经验：**不要假设 AI 会严格按照 JSON 类型传参。给它留一点容错空间，但不要无限宽容。**

### 第七步：升级你的工具

现在，让我们把上一章创建的工具升级一下。假设上一章的工具是一个简单的问候工具，接收一个名字然后返回问候语。我们给它加上完整的输入验证：

```typescript
import { z } from 'zod/v4'
import { lazySchema } from '../../utils/lazySchema.js'

const inputSchema = lazySchema(() =>
  z.strictObject({
    name: z
      .string()
      .min(1)
      .describe('The name of the person to greet. Must not be empty.'),
    language: z
      .enum(['zh', 'en', 'ja'])
      .optional()
      .describe(
        'Language for the greeting. Defaults to "en". Supported: "zh" (Chinese), "en" (English), "ja" (Japanese).',
      ),
    formal: semanticBoolean(
      z.boolean().default(false),
    ).describe('Whether to use formal greeting style. Defaults to false.'),
  }),
)
```

这个 schema 做了什么？

1. **`name`** 是必填字符串，`.min(1)` 确保不是空字符串。
2. **`language`** 是可选枚举，只能是 `"zh"`、`"en"`、`"ja"` 之一。描述里列出了所有选项和它们的含义。
3. **`formal`** 用 `semanticBoolean` 包裹，容忍 AI 传 `"true"/"false"` 字符串。`.default(false)` 表示不传时默认非正式风格。

然后，在 `validateInput()` 里做跨字段的验证：

```typescript
async validateInput(input) {
  // Zod 已经验证了基本类型，这里做业务逻辑验证
  if (input.name.length > 100) {
    return {
      result: false,
      message: 'Name is too long. Maximum 100 characters.',
      errorCode: 1,
    }
  }
  return { result: true }
},
```

`validateInput()` 是 Zod 之后的第二道防线。Zod 负责"形状对不对"（类型、枚举值、可选性），`validateInput()` 负责"语义对不对"（名字太长、文件不存在、参数互相矛盾等）。

### 第八步：验证错误是如何被 AI 看到的

当 AI 传了不合法的参数时，Zod 的验证错误会变成一条错误信息返回给 AI。比如 AI 传了：

```json
{ "name": "", "language": "fr" }
```

Zod 会生成类似这样的错误：

- `name`: 需要至少 1 个字符
- `language`: 不合法的枚举值。可选值为 'zh' | 'en' | 'ja'

AI 读到这条错误，会在下一轮自动修正参数重新调用。这就是为什么 `.describe()` 里的描述如此重要——好的描述让 AI 一次就传对参数，减少来回次数。

---

## 遇到问题？

### 问题 1：AI 总是传多余的字段

**症状**：你定义了 `z.strictObject`，AI 传了 schema 里没有的字段，工具调用失败。

**原因**：AI 有时候会"自作主张"，加上它认为有用的参数。

**解决方案**：有两个选择。如果你确定额外字段永远没用，保持 `z.strictObject`。如果你想让工具更宽容，换成 `z.object`——它允许额外字段存在（但你的 `call()` 方法会忽略它们）。Claude Code 的实践是：输入 schema 用 `z.strictObject`（严格），输出 schema 用 `z.object`（宽松）。因为输入来自 AI，需要严格把关；输出发给 AI，不需要那么严格。

### 问题 2：AI 把可选字段设成 null 或 "undefined"

**症状**：你用 `.optional()` 标记了一个字段，但 AI 传了 `{ path: null }` 或 `{ path: "undefined" }` 而不是直接省略这个字段。

**原因**：AI 不确定是否应该包含这个字段，就用了一个占位值。

**解决方案**：在 `.describe()` 里明确说明。Claude Code 的 GlobTool 就是这么做的：

```
DO NOT enter "undefined" or "null" - simply omit it for the default behavior.
```

这不是什么优雅的设计，但它管用。

### 问题 3：AI 传了错误的类型

**症状**：你期望一个数字，AI 传了一个字符串 `"30"`。或者你期望一个布尔值，AI 传了 `"false"`。

**原因**：AI 生成的 JSON 偶尔会有类型错误。

**解决方案**：使用 `semanticBoolean` 和 `semanticNumber` 包装器。对于其他类型的容错需求，你可以用 `z.preprocess` 自己写类似的逻辑。原则是：**宽容地接收常见错误，但不要无限宽容**。

### 问题 4：enum 的值需要动态变化

**症状**：你想让 `z.enum` 的可选值根据运行时状态变化（比如根据当前目录下的文件列表）。

**原因**：`z.enum` 要求编译时确定可选值。

**解决方案**：`z.string()` 加上 `validateInput()` 里的手动检查。`z.enum` 是声明式的（schema 自带验证），但不够灵活。如果枚举值需要动态生成，退回到 `z.string()` 然后在 `validateInput()` 里做判断。

---

## 验证

现在来测试一下你的理解。

**测试 1：合法输入。** 构造一个完整的合法参数：

```json
{
  "name": "Claude",
  "language": "zh",
  "formal": true
}
```

这个参数应该通过 Zod 验证，`call()` 正常执行。

**测试 2：省略可选字段。**

```json
{
  "name": "Claude"
}
```

`language` 和 `formal` 都被省略了。Zod 应该通过（都是 optional），`call()` 使用默认值。

**测试 3：不合法的枚举值。**

```json
{
  "name": "Claude",
  "language": "klingon"
}
```

Zod 应该拒绝，错误信息会提示可选值是 `'zh' | 'en' | 'ja'`。

**测试 4：空字符串。**

```json
{
  "name": "",
  "language": "en"
}
```

`.min(1)` 应该拒绝空字符串。

**测试 5：多余字段。**

```json
{
  "name": "Claude",
  "age": 30
}
```

`z.strictObject` 应该拒绝 `age` 字段，因为它不在 schema 定义里。

**测试 6：类型容错。**

```json
{
  "name": "Claude",
  "formal": "true"
}
```

`semanticBoolean` 应该把字符串 `"true"` 转成布尔值 `true`，验证通过。

如果这六个场景都按预期工作，你的输入验证就足够健壮了。

---

## 回顾

这一章，我们学会了用 Zod 为工具设计输入验证：

1. **`z.strictObject`** 是 Claude Code 的默认选择——拒绝多余字段，防止 AI 的"幻觉"参数。
2. **`.describe()`** 不只是给人看的注释，它是给 AI 的使用指南。好的描述能减少 AI 传错参数的概率。
3. **`.optional()`** 标记可选字段，但要在描述里明确告诉 AI 怎么处理省略。
4. **`z.enum`** 限制参数为固定值，是防止 AI "发明"选项的最佳方式。
5. **`z.array`** 处理列表类型的输入。
6. **`semanticBoolean` 和 `semanticNumber`** 处理 AI 偶尔的类型错误——宽容但不放纵。
7. **`lazySchema`** 延迟 schema 创建，避免模块加载时的循环依赖。
8. **`validateInput()`** 是 Zod 之后的第二道防线，处理跨字段验证和业务逻辑检查。

记住，输入验证不是一堵墙——它是一个对话。Zod 拦住了不合法的参数，告诉 AI 哪里错了，AI 在下一轮自动修正。好的 schema 设计让这个对话尽量短。

下一章，我们给工具加上权限规则——不是所有输入合法的操作都应该被执行。

---

[上一章：创建你的第一个工具](./第03章-创建你的第一个工具.md) | [下一章：添加权限规则](./第05章-添加权限规则.md)
