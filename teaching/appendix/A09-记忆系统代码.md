# A09 - 记忆系统代码详解

> **本文档目标**：深入解析 Claude Code 记忆系统的核心代码，包括会话持久化、CLAUDE.md 加载、自动记忆提取、会话记忆等。

---

## 1. 会话存储：Session Storage

**文件**：`src/utils/sessionStorage.ts:50-150`

**功能**：管理会话消息的 JSONL 持久化。

### 1.1 JSONL 格式

```
~/.claude/projects/<path>/sessions/<session-id>.jsonl

每行一条记录，type 字段区分类型：
{"type":"user","content":"帮我修复 bug","id":"msg-001","timestamp":1234567890}
{"type":"assistant","content":"好的，我来帮你","id":"msg-002","timestamp":1234567900}
{"type":"tool_result","toolUseId":"tu-001","content":"...","id":"msg-003"}
{"type":"compact_boundary","id":"cb-001","timestamp":1234568000}
{"type":"summary","content":"用户请求修复 bug，助手执行了...","id":"msg-004"}
```

### 1.2 写入消息

```typescript
// src/utils/sessionStorage.ts:50
export async function appendMessageToSession(
  sessionId: string,
  message: Message
): Promise<void> {
  const sessionPath = getSessionPath(sessionId)

  // 序列化为单行 JSON
  const line = JSON.stringify(message)

  // 追加写入
  await fs.appendFile(sessionPath, line + '\n')
}

// 读取会话消息
export async function readSessionMessages(
  sessionId: string
): Promise<Message[]> {
  const sessionPath = getSessionPath(sessionId)

  if (!await fs.pathExists(sessionPath)) {
    return []
  }

  const content = await fs.readFile(sessionPath, 'utf-8')
  const lines = content.split('\n').filter(line => line.trim())

  return lines.map(line => JSON.parse(line))
}
```

---

## 2. CLAUDE.md 加载引擎

**文件**：`src/utils/claudemd.ts:50-200`

**功能**：加载和合并多层级的 CLAUDE.md 指令文件。

### 2.1 加载层级（从低到高优先级）

```typescript
// src/utils/claudemd.ts:50
const LOAD_ORDER = [
  'managed',    // /etc/claude-code/CLAUDE.md
  'user',       // ~/.claude/CLAUDE.md
  'project',   // 从根目录到 CWD 的所有 CLAUDE.md
  'local',      // CLAUDE.local.md
  'automem',    // 自动记忆文件
] as const
```

### 2.2 @include 指令处理

```typescript
// src/utils/claudemd.ts:100
export function processIncludes(
  content: string,
  baseDir: string,
  processedFiles: Set<string> = new Set()
): string {
  // 正则匹配 @include 指令
  const includePattern = /@include\[([^\]]+)\]\((\S+)\)/g

  return content.replace(includePattern, (match, label, path) => {
    // 解析路径
    const resolvedPath = resolveIncludePath(path, baseDir)

    // 循环引用检测
    if (processedFiles.has(resolvedPath)) {
      console.warn(`Circular @include detected: ${resolvedPath}`)
      return ''
    }

    processedFiles.add(resolvedPath)

    // 读取并递归处理
    const includedContent = fs.readFileSync(resolvedPath, 'utf-8')
    return processIncludes(includedContent, pathDirname(resolvedPath), processedFiles)
  })
}

// 路径解析
function resolveIncludePath(path: string, baseDir: string): string {
  if (path.startsWith('@/')) {
    // @/absolute/path → 绝对路径
    return path.slice(2)
  }
  if (path.startsWith('@~/')) {
    // @~/home/path → home 目录
    return pathJoin(os.homedir(), path.slice(3))
  }
  if (path.startsWith('@./')) {
    // @./relative/path → 相对于 baseDir
    return pathJoin(baseDir, path.slice(3))
  }
  // @path → 相对于 baseDir
  return pathJoin(baseDir, path)
}
```

### 2.3 MCP 服务器定义检测

```typescript
// src/utils/claudemd.ts:150
export interface ClaudeMdMetadata {
  mcpServers?: MCPServerConfig[]
  hooks?: HookConfig[]
  agentType?: string  // 限制特定 agent 类型可见
}

export function parseClaudeMdFrontmatter(
  content: string
): { metadata: ClaudeMdMetadata; body: string } {
  const frontmatterPattern = /^---\n([\s\S]*?)\n---\n([\s\S]*)$/
  const match = content.match(frontmatterPattern)

  if (!match) {
    return { metadata: {}, body: content }
  }

  const [, frontmatter, body] = match
  const metadata = yaml.parse(frontmatter)

  return { metadata, body }
}
```

---

## 3. 会话记忆提取：Session Memory

**文件**：`src/services/SessionMemory/sessionMemory.ts:50-150`

**功能**：在会话结束时提取记忆，写入会话记忆文件。

### 3.1 触发条件检查

```typescript
// src/services/SessionMemory/sessionMemory.ts:50
export function shouldExtractMemory(messages: Message[]): boolean {
  const currentTokenCount = tokenCountWithEstimation(messages)

  // 1. 检查是否达到初始化阈值
  if (!isSessionMemoryInitialized()) {
    if (!hasMetInitializationThreshold(currentTokenCount)) {
      return false
    }
    markSessionMemoryInitialized()
  }

  // 2. 检查是否达到更新阈值
  const hasMetTokenThreshold = hasMetUpdateThreshold(currentTokenCount)

  // 3. 检查工具调用次数
  const toolCallsSinceLast = countToolCallsSince(
    messages,
    lastMemoryMessageUuid
  )
  const hasMetToolCallThreshold = toolCallsSinceLast >= getToolCallsBetweenUpdates()

  // 4. 检查最后 assistant turn 是否有工具调用（安全点）
  const hasToolCallsInLastTurn = hasToolCallsInLastAssistantTurn(messages)

  // 触发条件：token 阈值 + (工具调用阈值 OR 安全点)
  return hasMetTokenThreshold &&
    (hasMetToolCallThreshold || !hasToolCallsInLastTurn)
}
```

### 3.2 提取执行

```typescript
// src/services/SessionMemory/sessionMemory.ts:100
export async function extractSessionMemory(
  context: ToolUseContext
): Promise<void> {
  const messages = context.messages

  // 1. 获取当前记忆内容（如果存在）
  const currentMemory = await getCurrentSessionMemory()

  // 2. 构建提取 prompt
  const prompt = buildSessionMemoryUpdatePrompt({
    currentMemory,
    recentMessages: getRecentMessages(messages, 50),
    userContext: await getUserContext(context),
  })

  // 3. 限制工具权限（只允许编辑记忆文件）
  const restrictedCanUseTool = createMemoryFileCanUseTool(
    getSessionMemoryPath(context.sessionId)
  )

  // 4. 运行 fork agent
  await runForkedAgent({
    prompt,
    canUseTool: restrictedCanUseTool,
    cacheSafeParams: createCacheSafeParams(context),
  })
}
```

---

## 4. 持久记忆提取：Auto Memory

**文件**：`src/services/extractMemories/extractMemories.ts:50-150`

**功能**：从对话历史中提取持久记忆到 markdown 文件。

### 4.1 提取策略

```typescript
// src/services/extractMemories/extractMemories.ts:50
export async function extractAutoMemories(
  context: ToolUseContext
): Promise<void> {
  // 1. 扫描现有记忆文件
  const existingMemories = await scanMemoryFiles(getAutoMemDir())
  const manifest = formatMemoryManifest(existingMemories)

  // 2. 获取最近消息
  const recentMessages = getRecentMessages(context.messages, 100)

  // 3. 构建提取 prompt
  const prompt = buildExtractPrompt({
    existingMemories: manifest,
    newMessageCount: recentMessages.length,
    instructions: EXTRACTION_INSTRUCTIONS,
  })

  // 4. 运行 fork agent（严格限制工具）
  const result = await runForkedAgent({
    prompt,
    canUseTool: createAutoMemoryCanUseTool(),
    // 只允许：FileRead, Grep, Glob, Bash(只读), FileEdit, FileWrite
  })

  // 5. 解析 agent 生成的文件写入
  await processGeneratedMemoryFiles(result)
}
```

### 4.2 记忆文件格式

```typescript
// src/services/extractMemories/extractMemories.ts:80
// 生成的记忆文件格式：
// ---
// type: User
// project: my-project
// extracted_at: 2024-01-15T10:30:00Z
// ---

// ## 用户偏好

// - 偏好使用 TypeScript
// - 测试使用 Vitest
// - 代码风格：函数式优先

// ---
// type: Project
// ---

// ## 项目约定

// - API 响应使用 camelCase
// - 组件使用 PascalCase
```

---

## 5. 会话恢复：Session Resume

**文件**：`src/commands/resume/resume.ts:50-120`

**功能**：恢复已保存的会话继续对话。

```typescript
// src/commands/resume/resume.ts:50
export async function resumeSession(
  sessionId: string
): Promise<void> {
  // 1. 读取 JSONL 文件
  const sessionPath = getSessionPath(sessionId)
  const messages = await readSessionMessages(sessionId)

  // 2. 反序列化消息
  const deserializedMessages = messages.map(deserializeMessage)

  // 3. 处理压缩边界
  const { preBoundary, postBoundary } = splitByCompactBoundary(deserializedMessages)

  // 4. 如果有摘要，加载摘要内容
  if (postBoundary.length === 0 && preBoundary.length > 0) {
    const summary = findSummaryMessage(preBoundary)
    if (summary) {
      contextMessages = [summary, ...loadCompactKeptMessages(preBoundary)]
    }
  } else {
    contextMessages = postBoundary
  }

  // 5. 恢复工具状态
  await restoreToolState(contextMessages)

  // 6. 启动 query 循环
  await runQueryLoop(contextMessages)
}
```

---

## 6. 关键源码文件索引

| 文件 | 关键函数 | 说明 |
|------|---------|------|
| `src/utils/sessionStorage.ts` | `appendMessageToSession()` | 写入会话消息 |
| `src/utils/sessionStorage.ts` | `readSessionMessages()` | 读取会话消息 |
| `src/utils/claudemd.ts` | `loadClaudeMd()` | 加载 CLAUDE.md |
| `src/utils/claudemd.ts` | `processIncludes()` | 处理 @include |
| `src/utils/claudemd.ts` | `parseClaudeMdFrontmatter()` | 解析 frontmatter |
| `src/services/SessionMemory/sessionMemory.ts` | `shouldExtractMemory()` | 触发条件检查 |
| `src/services/SessionMemory/sessionMemory.ts` | `extractSessionMemory()` | 会话记忆提取 |
| `src/services/extractMemories/extractMemories.ts` | `extractAutoMemories()` | 持久记忆提取 |
| `src/commands/resume/resume.ts` | `resumeSession()` | 会话恢复 |
| `src/memdir/paths.ts` | `getAutoMemDir()` | 记忆目录路径 |
