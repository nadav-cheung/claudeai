# A2 - 代码附录：核心源码解析

> **本章目标**：提供 Claude Code 核心源码文件的快速参考，包括文件路径、核心函数签名、数据结构定义。

---

## 1. 工具系统

### 1.1 Tool 接口定义

**文件**：`src/Tool.ts`

```typescript
// 工具接口 - 支持进度数据的泛型
interface Tool<
  Input extends JsonSchema,
  Output = unknown,
  Progress extends ToolProgressData = ToolProgressData
> {
  name: string;
  description: string;
  input_schema: Input;
  __brand?: 'Tool';

  // 可选方法
  render?(progress: Progress): string | null;
  getProgress?(output: Output): Progress | undefined;
}
```

### 1.2 工具注册表

**文件**：`src/tools.ts`

```typescript
// 获取所有基础工具
function getAllBaseTools(): Tool[];

// 按名称获取工具
function getTool(name: string): Tool | undefined;

// 获取所有工具名称
function getToolNames(): string[];
```

### 1.3 工具执行

**文件**：`src/services/tools/toolExecution.ts`

```typescript
// 工具执行入口
async function executeTool(
  params: ExecuteToolParams
): Promise<ExecuteToolResult>;

// 执行参数
interface ExecuteToolParams {
  name: string;
  input: unknown;
  toolContext: ToolContext;
}

// 权限检查
function checkToolPermission(
  tool: Tool,
  context: ToolContext
): PermissionResult;
```

---

## 2. 权限系统

### 2.1 权限模式

**文件**：`src/utils/permissions/permissions.ts`

```typescript
// 权限模式枚举
enum PermissionMode {
  BYPASS = 'bypass',           // 完全跳过
  FAIL = 'fail',               // 无权限直接失败
  PROMPT = 'prompt',           // 询问用户
  APPROVE = 'approve',         // 自动批准
}

// 权限检查结果
interface PermissionResult {
  allowed: boolean;
  reason?: string;
  requiresApproval?: boolean;
}
```

### 2.2 规则匹配

```typescript
// 规则引擎
class RuleEngine {
  // 检查工具是否允许执行
  check(toolName: string, input: unknown): PermissionResult;

  // 添加规则
  addRule(rule: PermissionRule): void;

  // 清除规则
  clearRules(): void;
}
```

### 2.3 拒绝追踪

**文件**：`src/utils/permissions/denialTracking.ts`

```typescript
// 拒绝追踪配置
const DENIAL_TRACKER_CONFIG = {
  maxConsecutive: 3,  // 连续拒绝次数上限
  maxTotal: 20,       // 总拒绝次数上限
};

// 追踪器
class DenialTracker {
  recordDenial(toolName: string): void;
  shouldDeny(toolName: string): boolean;
  reset(): void;
}
```

---

## 3. 上下文管理

### 3.1 消息类型

**文件**：`src/state/messages.ts`

```typescript
// 消息基础类型
interface BaseMessage {
  id: string;
  timestamp: number;
  type: string;
}

// 用户消息
interface UserMessage extends BaseMessage {
  type: 'user';
  content: string;
}

// AI 消息
interface AssistantMessage extends BaseMessage {
  type: 'assistant';
  content: ContentBlock[];
  toolCalls?: ToolCall[];
  usage?: Usage;
}

// 工具结果消息
interface ToolResultMessage extends BaseMessage {
  type: 'tool_result';
  toolCallId: string;
  content: string;
}
```

### 3.2 压缩策略

**文件**：`src/services/compact/strategies.ts`

```typescript
// 压缩策略接口
interface CompactionStrategy {
  // 压缩消息数组
  compact(messages: Message[], maxTokens: number): CompactionResult;
}

// 可用策略
enum CompactionStrategyType {
  FULL = 'full',              // 完全压缩
  MICRO = 'micro',            // 微压缩
  SESSION_MEMORY = 'session_memory',  // 会话记忆压缩
}
```

### 3.3 Token 计算

```typescript
// Token 估算
function estimateTokens(text: string): number;

// 消息数组 token 统计
function calculateMessagesTokenCount(messages: Message[]): {
  inputTokens: number;
  outputTokens: number;
  totalTokens: number;
};
```

---

## 4. MCP 协议

### 4.1 连接配置

**文件**：`src/services/mcp/connection.ts`

```typescript
// MCP 连接类型
type ConnectionType = 'stdio' | 'sse' | 'http' | 'websocket';

// 连接配置
interface McpConnectionConfig {
  type: ConnectionType;
  command?: string;
  args?: string[];
  env?: Record<string, string>;
  url?: string;
}

// 初始化 MCP 连接
async function initializeMcpConnection(
  config: McpConnectionConfig
): Promise<McpConnection>;
```

### 4.2 工具暴露

```typescript
// MCP 工具转换为 Claude Code 工具
function mcpToolToTool(mcpTool: McpTool): Tool;

// 暴露工具到 AI
function exposeTools(connection: McpConnection): void;
```

---

## 5. Agent 系统

### 5.1 任务类型

**文件**：`src/services/agents/types.ts`

```typescript
// 本地 Agent 任务
interface LocalAgentTask {
  type: 'local';
  prompt: string;
  tools: Tool[];
  parentContext?: AgentContext;
}

// 进程内队友任务
interface InProcessTeammateTask {
  type: 'in_process_teammate';
  taskId: string;
  message: string;
}

// 远程 Agent 任务
interface RemoteAgentTask {
  type: 'remote';
  agentId: string;
  task: string;
}
```

### 5.2 Agent 启动

```typescript
// 启动 Agent
async function runAgent(task: AgentTask): Promise<AgentResult>;

// Agent 结果
interface AgentResult {
  success: boolean;
  output?: string;
  error?: string;
}
```

---

## 6. API 通信

### 6.1 消息流

**文件**：`src/services/api/claude.ts`

```typescript
// 创建消息流
async function* createMessageStream(
  params: CreateMessageParams
): AsyncGenerator<StreamEvent>;

// 参数
interface CreateMessageParams {
  model: string;
  messages: Message[];
  systemPrompt?: string;
  tools?: Tool[];
  maxTokens?: number;
}

// 标准化消息
function normalizeMessagesForAPI(
  messages: Message[]
): NormalizedMessage[];
```

### 6.2 重试策略

**文件**：`src/services/api/withRetry.ts`

```typescript
// 重试选项
interface RetryOptions {
  maxRetries?: number;        // 默认 3
  initialDelayMs?: number;     // 默认 1000
  backoffMultiplier?: number; // 默认 2
}

// 带重试执行
async function withRetry<T>(
  fn: () => Promise<T>,
  options?: RetryOptions
): Promise<T>;
```

### 6.3 OAuth 认证

**文件**：`src/services/oauth/client.ts`

```typescript
// OAuth PKCE 认证
async function authenticateWithOAuth(): Promise<OAuthTokens>;

// Token 交换
async function exchangeCodeForToken(
  code: string,
  codeVerifier: string
): Promise<OAuthTokens>;
```

---

## 7. Skills 系统

### 7.1 Skill 来源

**文件**：`src/skills/loadSkillsDir.ts`

```typescript
// 获取所有 Skill 命令
async function getSkillDirCommands(cwd: string): Promise<Command[]>;

// Skill 来源优先级
enum SkillSourcePriority {
  BUNDLED = 1,
  MANAGED = 2,
  USER = 3,
  PROJECT = 4,
  ADDITIONAL = 5,
  COMMANDS = 6,  // legacy
}
```

### 7.2 Bundled Skills

**文件**：`src/skills/bundledSkills.ts`

```typescript
// 注册 Bundled Skill
function registerBundledSkill(
  definition: BundledSkillDefinition
): void;

// 获取所有 Bundled Skills
function getBundledSkills(): Command[];
```

---

## 8. Plugin 系统

### 8.1 生命周期操作

**文件**：`src/services/plugins/pluginOperations.ts`

```typescript
// 安装 Plugin
async function installPluginOp(
  plugin: string,
  scope?: InstallableScope
): Promise<PluginOperationResult>;

// 启用/禁用
async function setPluginEnabledOp(
  pluginId: string,
  enabled: boolean
): Promise<PluginOperationResult>;

// 更新
async function updatePluginOp(
  pluginId: string
): Promise<PluginOperationResult>;

// 卸载
async function uninstallPluginOp(
  pluginId: string
): Promise<PluginOperationResult>;
```

### 8.2 Hook 加载

**文件**：`src/utils/plugins/loadPluginHooks.ts`

```typescript
// 加载所有 Plugin Hooks
function loadPluginHooks(): void;

// 清除已注册的 Hooks
function clearRegisteredPluginHooks(): void;
```

---

## 9. 会话与状态

### 9.1 会话管理

**文件**：`src/state/sessions.ts`

```typescript
// 会话状态
interface SessionState {
  id: string;
  messages: Message[];
  contextTokens: number;
  lastActive: number;
}

// 创建会话
function createSession(): SessionState;

// 恢复会话
function restoreSession(id: string): Promise<SessionState | null>;
```

### 9.2 状态持久化

```typescript
// 保存状态到磁盘
async function saveState(state: AppState): Promise<void>;

// 从磁盘加载
async function loadState(): Promise<AppState | null>;
```

---

## 10. 数据结构速查表

### 10.1 ContentBlock

```typescript
type ContentBlock =
  | { type: 'text'; text: string }
  | { type: 'tool_use'; id: string; name: string; input: Record<string, unknown> }
  | { type: 'tool_result'; tool_use_id: string; content: string };
```

### 10.2 ToolCall

```typescript
interface ToolCall {
  id: string;
  type: 'tool_use';
  name: string;
  input: Record<string, unknown>;
}
```

### 10.3 Usage

```typescript
interface Usage {
  inputTokens: number;
  outputTokens: number;
  totalTokens: number;
}
```

### 10.4 Error

```typescript
interface Error {
  type: string;
  message: string;
}
```

---

## 11. 文件路径映射

| 功能 | 源码路径 |
|------|----------|
| 工具接口 | `src/Tool.ts` |
| 工具注册 | `src/tools.ts` |
| 工具执行 | `src/services/tools/toolExecution.ts` |
| 权限规则 | `src/utils/permissions/permissions.ts` |
| 拒绝追踪 | `src/utils/permissions/denialTracking.ts` |
| 消息状态 | `src/state/messages.ts` |
| 上下文压缩 | `src/services/compact/strategies.ts` |
| API 调用 | `src/services/api/claude.ts` |
| 重试逻辑 | `src/services/api/withRetry.ts` |
| OAuth | `src/services/oauth/client.ts` |
| MCP 连接 | `src/services/mcp/connection.ts` |
| Agent 类型 | `src/services/agents/types.ts` |
| Skills 加载 | `src/skills/loadSkillsDir.ts` |
| Bundled Skills | `src/skills/bundledSkills.ts` |
| Plugin 操作 | `src/services/plugins/pluginOperations.ts` |
| Plugin Hooks | `src/utils/plugins/loadPluginHooks.ts` |

`★ Insight ─────────────────────────────────────`
这个附录是源码的"快照"——真实的接口定义、函数签名、数据结构。当你在阅读源码时遇到不熟悉的类型，可以在这里快速查找。但要注意，源码会演进，这些定义可能随版本变化。
`─────────────────────────────────────────────────`
