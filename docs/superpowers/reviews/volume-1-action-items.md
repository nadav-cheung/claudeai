# 卷一优化清单

> 来源：评审报告 + 三专家会审结论
> 优先级：P0（必须改）> P1（强烈建议）> P2（锦上添花）

---

## 全局修改（影响全部章节）

| # | 优先级 | 修改项 | 具体操作 |
|---|--------|--------|---------|
| G1 | **P0** | 删除元信息 | 删除每章开头 5-13 行的"本章内部结构模板（卷一统一）：…"整段文字（ch09 没有此段，其余 9 章都有） |
| G2 | P1 | 行号→函数名 | 将 `query.ts:1062-1358` 类引用改为 `query.ts` 的 `queryLoop()` 函数。全文搜索 `:\d+` 模式批量替换 |
| G3 | P1 | "试一试"加预期输出 | 每个"试一试"的 console.log 修改后，加一段"运行后你应该看到类似输出：" |
| G4 | P2 | "简化版"标注收敛 | 全文约 52 处"简化版"标注。保留真正省略了重要逻辑的（约 10 处），其余删除 |
| G5 | P2 | 术语表/速查附录 | 附录目录当前为空。创建 `appendix/glossary.md`（术语表）和 `appendix/function-reference.md`（30-50 个核心函数的一句话描述+文件位置） |

---

## ch03 准备工具箱

| # | 优先级 | 修改项 | 具体操作 |
|---|--------|--------|---------|
| 3-1 | **P1** | 增加全局旅程预览图 | 在路线图之后、"知识补全"之前，加一张简化版八站全景图（不含细节，只有站名和箭头），让读者出发前看到全貌 |
| 3-2 | P2 | 增加读者自测 | 开头加能力自检清单："你应该具备：TypeScript async/await、React 基础概念、命令行使用经验" |

---

## ch04 输入捕获

| # | 优先级 | 修改项 | 具体操作 |
|---|--------|--------|---------|
| 4-1 | P2 | 扩展附件系统说明 | 4.8 节"附件系统"偏简略，补充 @agent-xxx 提及的具体工作流程和 IDE 选择的完整示例 |

---

## ch05 系统提示

| # | 优先级 | 修改项 | 具体操作 |
|---|--------|--------|---------|
| 5-1 | P1 | 明确列出所有动态 section | 当前用"12+"模糊表述。将 5.1 节代码中列出的所有 `systemPromptSection(...)` 调用整理为编号列表 |
| 5-2 | P2 | CLAUDE.md @include 实例 | 5.5 节描述了 @include 语法但没有完整示例，补充一个 2-3 层嵌套 include 的实际案例 |

---

## ch07 API 调用

| # | 优先级 | 修改项 | 具体操作 |
|---|--------|--------|---------|
| 7-1 | **P1** | 开头加难度提示 | 章节标题后加一段提示："本章是全书信息密度最高的一章，涉及 AsyncGenerator、流式响应、上下文压缩等概念。建议先通读一遍建立整体印象，再逐段精读。" |
| 7-2 | **P1** | 先骨架再展开 | 在 7.2 节之前，先用 20 行伪代码画出 `queryLoop` 骨架：`while(true) { 上下文管理 → API调用 → 流式处理 → 工具执行 → 状态更新 }`，再逐段展开 |

---

## ch08 工具执行

无明显问题。保持现状。

---

## ch09 循环与状态（重点优化章）

| # | 优先级 | 修改项 | 具体操作 |
|---|--------|--------|---------|
| 9-1 | **P0** | 扩展至 350+ 行 | 当前 224 行，为全书最短。目标扩展到至少 350 行 |
| 9-2 | **P0** | 三阶段恢复展开为独立小节 | 当前三阶段错误恢复（9.3 节）合并在一起，拆为：`9.3 阶段一：Context Collapse drain`、`9.4 阶段二：Reactive Compact`、`9.5 阶段三：Max Output Token 升级`，每节包含原理说明 + 代码示例 + 适用场景 |
| 9-3 | **P0** | 加与 ch07 的关系说明 | 开头加一段："ch07 的四层压缩管线（Budget → Snip → Microcompact → Autocompact）在每轮 API 调用**之前**执行，目的是预防上下文溢出。本章描述的是循环**退出时**的错误恢复——当 API 调用已经失败后的补救措施。两者互补。" |
| 9-4 | P1 | 补充"本章内部结构模板" | ch09 是唯一缺少八段式模板的章节，补齐与其他章节一致的结构 |
| 9-5 | P1 | 补充调试实践和试一试 | 当前调试实践只有一个表格和一个 console.log，与其他章节（通常有 5+ 断点位置和 2-3 个试一试修改）差距明显 |

---

## ch10 渲染输出

| # | 优先级 | 修改项 | 具体操作 |
|---|--------|--------|---------|
| 10-1 | P2 | Reconciler 知识补全加实例 | 10 站的知识补全偏抽象，补充一个最小 Reconciler 示例（10 行代码展示 createInstance/appendChildInstance） |

---

## ch11 权限与安全

| # | 优先级 | 修改项 | 具体操作 |
|---|--------|--------|---------|
| 11-1 | P2 | 拆分权限管线图 | 11.2 节的分层检查管线图过于密集，拆为两张：图 A"规则检查阶段（Step 1）"、图 B"模式决策与回退（Step 2-3）"，中间用"如果 Step 1 没有 deny，进入 Step 2"连接 |
| 11-2 | P2 | 加图注 | 每张 Mermaid 图后加一行说明文字 |

---

## ch12 旅程复盘

| # | 优先级 | 修改项 | 具体操作 |
|---|--------|--------|---------|
| 12-1 | P2 | 增加"推荐重读路线" | 在"卷一→卷二映射表"之后加一段："重读建议——如果你对工具执行感兴趣，第二遍重点看 ch07+ch08；如果你对安全感兴趣，重点看 ch11；如果你想做贡献，按 ch03→ch04→ch07→ch08→ch12 的顺序精读" |
| 12-2 | P2 | 增加可验证的自检题 | 将检查点中的"你现在已经理解了 X"改为可操作的题目，如"不看书，画出 StreamingToolExecutor 的并发分区算法流程图" |

---

## 按优先级排序的执行顺序

```
第一步（P0，约 2 小时）：
  G1 → 删除 9 章的元信息模板（批量 sed）
  9-1+9-2+9-3+9-4+9-5 → 重写 ch09（最耗时的单章修改）
  9-3 → 在 ch09 开头加与 ch07 的关系说明

第二步（P1，约 3 小时）：
  G2 → 全文行号替换为函数名
  3-1 → ch03 加全局预览图
  7-1+7-2 → ch07 加难度提示 + 骨架伪代码
  5-1 → ch05 明确动态 section 列表
  G3 → 所有"试一试"加预期输出

第三步（P2，约 2 小时）：
  G5 → 创建术语表和函数速查表
  12-1+12-2 → ch12 加重读路线和自检题
  11-1 → ch11 拆分管线图
  4-1+5-2+10-1 → 各章细节补全
```
